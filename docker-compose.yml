services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/tracker.db
      - AUTH_PASSWORD=changeme   # set your password here
    restart: unless-stopped